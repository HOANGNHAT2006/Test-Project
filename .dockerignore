# --- BỎ QUA FILE HỆ THỐNG & GIT ---
.git
.gitignore
.dockerignore
.DS_Store
Thumbs.db
.vscode/
.idea/

# --- BỎ QUA MÔI TRƯỜNG ẢO & LOGS ---
# Docker sẽ tự cài môi trường mới bên trong, không cần copy cái cũ ở máy bạn vào
node_modules/
.venv/
venv/
env/
ENV/
__pycache__/
*.py[cod]
*$py.class
*.log
npm-debug.log*
yarn-debug.log*

# --- BỎ QUA CÁC FILE BUILD FRONTEND ---
# Docker sẽ tự build lại
build/
dist/

# --- BẢO MẬT ---
# Không copy file chứa pass vào Image
.env
.env.local
*.pem
*.key

# --- LƯU Ý QUAN TRỌNG: MODEL AI ---
# Tôi ĐÃ XÓA các dòng *.keras, *.h5 ở đây.
# Để Docker có thể COPY file model vào trong container và chạy được.